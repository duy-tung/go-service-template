{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "order-engine.serviceAccountName" . }}
  labels:
    {{- include "order-engine.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
